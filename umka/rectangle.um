// Rectangle is used mainly as camera.

import (
	"th.um"	
)

type rect* = struct {
	x, y, w, h: th.fu
}

fn mk*(x, y, w, h: th.fu): rect {
	var r: rect

	r.x = x
	r.y = y
	r.w = w
	r.h = h

	return r
}
