
//type polygon* = int

// this line causes the issue:
fn newpoly*(x, y, vcount: int, v: []int): int

fn cpolysetpos(x, y: int, p: int)

fn setpos*(p, x, y: int) {
	cpolysetpos(x, y, p)
}

//fn (p ^poly) getpos*(): (int, int)
