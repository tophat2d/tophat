
import (
	"rectangle.um"
)

type cone* = struct {
	px: int32
	py: int32
	rot: int32
	lenght: int32
	width: int32
	color: uint32	
}

fn cdrawcone(c: ^cone, cam: ^rectangle.rect)

fn (c: ^cone) draw*(cam: rectangle.rect) {
	cdrawcone(c, &cam)
}
