
import (
	"th.um"
	"rect.um"
	"audio.um"
	"canvas.um"
	"window.um"
)

fn main() {
	window.setup("title", 1280, 720)

	cam := rect.mk(0, 0, 192, 108)

	s := audio.load("sample.ogg")

	s.play()
	for window.cycle(cam) {
		canvas.setColor(th.white)
		canvas.drawRect(cam)
	}
}
