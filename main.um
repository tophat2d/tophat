import (
	"th.um"
	"rect.um"
	"image.um"
	"window.um"	
)

fn main() {
	// TODO(skejeton): If we don't call this on windows, most of the following
	//				   tophat calls will cause a null dereference, because 
	//				   GLAD didn't initialize any functions.
	window.setup("Hello world", 100, 100)
	for window.cycle(rect.mk(0, 0, 100, 100)) { }
}
