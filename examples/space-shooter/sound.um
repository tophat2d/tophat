
import (
	"audio.um"
)

var (
	dmg*, explosion*, shoot*: audio.Sound
)

fn init*() {
	dmg = audio.load("res://sfx/dmgsound.wav")
	dmg.setVol(0.7)
	explosion = audio.load("res://sfx/explosion.wav")
	explosion.setVol(0.7)
	shoot = audio.load("res://sfx/shoot.wav")
	shoot.setVol(0.7)
}
